# Optional Docker Compose settings. No configuration is required to start.
# Copy this file to .env only when overriding the defaults.
PR0_PORT=3000
PR0_BIND_ADDRESS=0.0.0.0

# Additional browser origins; same-origin web and Tauri need no override.
API_ALLOWED_ORIGINS=
