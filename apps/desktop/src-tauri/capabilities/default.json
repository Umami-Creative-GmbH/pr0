{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window with no native command permissions until needed.",
  "windows": ["main"],
  "permissions": []
}
