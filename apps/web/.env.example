# Extra browser origins, comma separated. Tauri origins are allowed automatically.
# Local Vite origins are allowed automatically only in development.
API_ALLOWED_ORIGINS=

# Rebuildable private SQLite search projection. Keep writable and persistent.
# Relative paths resolve from apps/web; the Docker image uses /var/lib/pr0-search.
PR0_SEARCH_DIRECTORY=.data/search
